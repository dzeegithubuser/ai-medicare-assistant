Return a valid JSON array matching this schema exactly:

[
  {
    "npi": "pharmacy NPI from the list above",
    "drugs": [
      {
        "rxCui": "string",
        "drugName": "normalized drug name",
        "ndc": "NDC in format 00000-0000-00",
        "retailPrice": 285.50,
        "medicarePrice": 47.20,
        "genericPrice": null
      }
    ]
  }
]

Output rules:
- Use only the keys shown above.
- npi must match a provided pharmacy NPI exactly.
- retailPrice is required and must be a positive number.
- medicarePrice and genericPrice are nullable (use null when not applicable).
- ndc must be in format 00000-0000-00 or null if unknown.
- drugName must be the normalized drug name, not a brand alias.
